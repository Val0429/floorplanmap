<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:FloorPlanMap.Components.Backgrounds">

    <Style TargetType="{x:Type local:ImageBackground}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:ImageBackground}">

                    <Image x:Name="ImageBackground" Source="{Binding MapSource}" Panel.ZIndex="-10">
                        <Image.CacheMode>
                            <BitmapCache RenderAtScale="1" />
                        </Image.CacheMode>
                    </Image>

                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
